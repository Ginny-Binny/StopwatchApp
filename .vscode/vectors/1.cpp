#include <bits/stdc++.h>
using namespace std;
int main(){
    vector<int> v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    for(int i; i<v.size(); i++)
    {

        
        v.push_back(v[i]);
    }
    cout<<vector.begin();
    
return 0;
}